这份文档为你梳理了整个Aime 读卡器通讯桥接的核心逻辑与协议细节，非常适合用于向 C/C++ 或 Rust 环境（例如 STM32 或其他单片机）进行底层固件移植。
Aime 读卡器通讯桥接与移植参考手册
1. 系统架构概述
本系统作为物理读卡模块（PN532）与上位机游戏之间的中间件。其核心任务是维护一个双向非阻塞的通讯桥梁，将物理卡片（FeliCa / MIFARE）的底层硬件数据，转换为游戏可识别的 Aime 协议数据块。
1.1 硬件拓扑
* 接口 A (底层读卡器): UART 串口连接 PN532 模块。
* 接口 B (上位机通讯): UART 串口连接游戏主机。
* 核心模块: 包含 PN532 状态机、游戏应答引擎、以及一块由互斥锁（或禁用中断）保护的共享内存（Card State）。
1.2 串口通讯规范（双端一致）
* 波特率 (Baud Rate): 115200 bps
* 数据位 (Data Bits): 8
* 停止位 (Stop Bits): 1
* 校验位 (Parity): None
2. 核心数据结构与状态转换
在进行嵌入式或跨语言移植时，必须建立全局的共享状态来解耦两侧的串口收发。
2.1 共享卡片状态 (CardState)
用于暂存物理读卡器读取到的有效数据，供游戏端随时拉取。
* bool is_present: 卡片是否在线（需配合超时机制维护）。
* uint8_t block_data[16]: 标准的 16 字节 Aime 数据块。
* uint32_t last_read_time: 硬件最后一次成功寻卡的时间戳（用于防抖和离线判定）。
2.2 离线判定与防抖机制 (关键)
* 底层物理防抖: PN532 对同一张卡的连续读取间隔必须限制在 1秒~1.5秒 以上，防止模块过载死机。
* 逻辑在线超时: 上位机接口判断卡片在线的超时时间（Timeout）应设定为 2.0秒~2.5秒（必须大于物理防抖时间），以确保在防抖间隔内，游戏侧仍认为卡片未移开。
3. PN532 通讯协议细则
PN532 侧的移植必须采用状态机模式运行，严禁在发送验证指令后死等（Blocking Wait）回复。
3.1 冷启动与唤醒序列
模块上电或复位后，必须执行唤醒操作：
1. 拉低总线: 发送包含 0x55 0x55 的低电平脉冲串唤醒休眠。
2. 清空缓冲: 强制清空（Flush）UART 接收寄存器中的自检乱码。
3. 配置指令: 依次发送 SAM 配置、最大重试次数配置等。
3.2 寻卡与数据提取机制
主循环中需交替发送长轮询（支持所有卡种）和短轮询指令。
场景 A: 发现 FeliCa 原生卡 (0x14)
* 提取目标: 数据包中的 8 字节 IDm。
* 数据转换: 将 8 字节 IDm 视为大端十六进制数，转换为 20位十进制字符串。
* 构建 Block: 将这 20 位字符串放入 16 字节数组的后 10 个字节，前 6 个字节补 0x00。存入共享内存。
场景 B: 发现 MIFARE 兼容卡
* 第一步 (AUTHING): 提取物理 UID。发送秘钥认证指令验证 Block 2。
   * AimeKey (Hex): 57 43 43 46 76 32
* 第二步 (READING): 认证成功后，发送读取 Block 2 指令。
* 提取目标: 将读取到的 16 字节完整数据直接存入共享内存。
3.3 异常恢复看门狗
在固件中必须实现状态看门狗。如果状态机处于 AUTHING 或 READING 状态超过 500ms 未收到 PN532 响应（UART 丢包或卡片瞬间抽离），必须强制将状态机复位为 IDLE，否则将导致死锁，模块停止寻卡。
4. 游戏端 Aime 协议通讯细则
游戏端的通讯为严格的“一问一答”模式。所有的封包（Packet）必须经过转义与校验。
4.1 封包转义规则 (HDLC-like)
* 帧头: 0xE0
* 转义控制符: 0xD0
* 规则: 封装载荷时，遇到 0xE0 或 0xD0，必须插入 0xD0，并将原字节减 1。
   * 例如: 0xE0 变为 0xD0 0xDF。
4.2 核心指令响应表
游戏下发指令 (CMD)
	指令含义
	处理逻辑与应答 (Response)
	0x30
	获取固件版本
	返回固定载荷: 0x94
	0x32
	获取硬件版本
	返回固定载荷: "837-15396"
	0x42
	卡片探测 (Card Detect)
	查询共享状态 is_present。如果有卡，返回 01 10 04 01 02 03 04；无卡返回 00。
	0x52
	读取扇区 (Mifare Read)
	检查请求的 Block 号。如果是 Block 2，返回共享状态中的 16 字节 block_data；否则返回 16 字节全 0x00。
	0xF0
	获取扩展板信息
	返回固定载荷: "000-00000\xFF\x11\x40"
	(注：对于 0x81, 0x82 的 LED 控制指令，通常无需应答，直接丢弃即可。)
5. 嵌入式移植特别建议 (MCU / RTOS)
如果你打算将此逻辑移植到单片机（如 STM32 系列）上，请特别关注以下几点：
1. 中断与 DMA 接收: 游戏协议帧以 0xE0 开头，建议配置 UART 开启 DMA 环形缓冲区（Ring Buffer）加空闲中断（IDLE Interrupt），以完整接收不定长的转义数据包。
2. 资源互斥锁 (Mutex): 如果你在 RTOS 下使用两个独立 Task 分别处理 PN532 和游戏通讯，对“共享卡片状态”的读写必须使用互斥量或关闭全局中断进行保护，防止脏读。
3. Tx 发送保护: PN532 的发送端口必须加锁。由于状态机中的认证指令和主循环的轮询指令可能会产生竞态条件，同时写入 UART 的 Tx 寄存器会导致数据包交错损坏。